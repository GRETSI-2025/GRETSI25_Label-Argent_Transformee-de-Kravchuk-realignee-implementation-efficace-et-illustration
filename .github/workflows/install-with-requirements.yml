name: test installation of package using requirements.txt

on: [push, pull_request]

jobs:
  install-repo:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ['3.11']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Load cached venv
      id: cached-dependencies
      uses: actions/cache@v4
      with:
        path: .venv
        key: venvdotrequirements-${{ runner.os }}-${{ hashFiles('.github/actions/install-env/action.yml') }}-${{ steps.setup-python.outputs.python-version }}

    - name: Load cached .local
      id: cached-dotlocal
      uses: actions/cache@v4
      with:
        path: ~/.local
        key: dotlocal-${{ runner.os }}-${{ hashFiles('.github/actions/install-env/action.yml') }}-${{ steps.setup-python.outputs.python-version }}


    - name: Install dependencies with requirements.txt
      shell: bash
      run: pip install -r requirements.txt

    - name: run preamble to test
      run: |
        python --version
        python src/preamble.py
